{
  "name": "emergency-response-mobile-app",
  "version": "1.0.0",
  "description": "Mobile app for the Emergency Response Demo",
  "main": "server.js",
  "scripts": {
    "start": "tsc && node set-config.js && node dist/server.js",
    "build": "cd client && (npm install && npm run build)",
    "build-dev": "cd client && (npm run build)",
    "dev": "nodemon server.js"
  },
  "license": "MIT",
  "dependencies": {
    "@types/express": "^4.17.11",
    "express": "^4.17.1",
    "http-proxy-middleware": "^1.0.6",
    "password-generator": "^2.3.2",
    "replace": "^1.2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7",
    "typescript": "^4.2.2"
  }
}
